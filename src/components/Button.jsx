const Button = () => {
  return (
    <div>
      <button
        className="bg-gray-700 px-4 py-2 cursor-pointer rounded-full text-gray-50 hover:bg-gray-600 transition-colors duration-300"
        type="submit"
      >
        Gerar Documentos Selecionados
      </button>
    </div>
  );
};

export default Button;
